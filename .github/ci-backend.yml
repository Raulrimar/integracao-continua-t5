name: primemeiro Workflow
on:
   push:
jobs:
  primeiro-job:
    runs-on: ubuntu-latest
    steps:
        -run: echo "primeiro Passo"
        -name: Checkout
         uses: actions/checkt@v4
        -name: lista arquivos
         run: ls ${{ github.Workspace }}
        -name: Status
         run: echo " Status ${{ job.status}}."